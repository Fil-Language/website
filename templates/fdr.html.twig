{% extends 'base.html.twig' %}

{% block title %}| FDR{% endblock %}

{% block body %}
    {% include('navbar.html.twig') %}

    <main class="content-container">
        <section class="content-wrapper">
            <div class="content">
                <h2>FDR</h2>
                {% if records is empty %}
                    <p>Nothing to show</p>
                {% else %}
                    <div>
                        {% for fdr in records %}
                            <a href="{{ fdr.link }}">{{ fdr.name }}</a>
                        {% endfor %}
                    </div>
                {% endif %}
            </div>
        </section>
    </main>

    {% include('footer.html.twig') %}
{% endblock %}